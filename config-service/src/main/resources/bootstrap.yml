server:
  port: 8888
spring:
  profiles:
    active: native

---

server:
  port: 8888
spring:
  profiles: docker
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config