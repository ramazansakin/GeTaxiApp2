server:
  port: 8888
spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ramazansakin/GeTaxiApp2/tree/master/config-service
          search-paths: /config