spring:
  profiles:
    active: native
  application:
    name: discovery-service

server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false

---

spring:
  profiles: docker
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://config-service:8888